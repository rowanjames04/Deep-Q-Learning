# Direct dependencies (pinned for reproducibility).
# tensorflow and torchvision were previously listed but unused — removed.
torch==2.12.1
gymnasium==1.3.0
flappy-bird-gymnasium==0.4.0
pyyaml==6.0.3
matplotlib==3.11.0
numpy==2.5.0